/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4 — replaces the old `tailwindcss: {}` entry
    "@tailwindcss/postcss": {},
  },
};

export default config;
